# Frontend environment — copy to .env and adjust per environment.
# VITE_* vars are baked into the bundle at build time.

# URL of the Everline API (Express) — same host, different port locally.
VITE_API_URL=http://localhost:3001
